let fruit=["Apple","Grapes","Banana"];
fruit.sort();
console.log(fruit);

let number=[1,10,23,2]
console.log(number.sort());
number.sort((a,b)=>a-b)//assending order
console.log(number);
number.sort((a,b)=>b-a)//desending order
console.log(number);